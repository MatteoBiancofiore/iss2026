package main.java.conway.domain;

public class Life implements LifeInterface {

	private final int DIM = 5;

	// grid for current and next Generation

	private Grid currentGen;
	private Grid nextGen;

	public Life() {
		this.currentGen = new Grid(DIM, DIM);
		this.nextGen = new Grid(DIM, DIM);
	}

	private int countPopulation(int row, int col) {
		int res = 0;

		if (row != 0 && row != 4 && col != 0 && col != 4) {

			for (int i = row - 1; i < row + 1; i++) {
				for (int j = col - 1; j < col + 1; j++) {

					if (i == row && j == col)
						continue;

					if (this.currentGen.getCell(row, col).getStatus())
						res++;
				}

			}

		}

		return res;
	}

	@Override
	public void nextGen() {

	}

	@Override
	public void show() {
		// TODO Auto-generated method stub

	}

}
